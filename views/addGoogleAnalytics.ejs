<div class="container">
    <div class="row mt-5">
      <div class="col-md-6 m-auto">
        <div class="card card-body">
          <h1 class="text-center mb-3">
            <i class="fas fa-folder-plus"></i> Add Google Analytics
          </h1>
          <%- include('layouts/partials/messages') %>
          <form action="/admin/google-analytics/add" method="POST">
            <div class="form-group">
                <label for="name">Code for <strong>head tag</strong> *</label>
                <textarea name="head" id="head" cols="30" rows="10" class="form-control"><%= typeof head != 'undefined' ? head : '' %></textarea>
            </div>
            <div class="form-group">
                <label for="name">Code for <strong>body tag</strong> *</label>
                <textarea name="body" id="body" cols="30" rows="10" class="form-control"><%= typeof body != 'undefined' ? body : '' %></textarea>
            </div>
            <button type="submit" class="btn btn-primary btn-block">Add</button>
          </form>
        </div>
      </div>
    </div>
</div>
  